export enum Pathnames {
  Dashboard = '/',
  Homepage = '/homepage',
  Login = '/login',
  Profile = '/profile',
  Register = '/register',
}

export enum endpoints {
  Contact = 'contacts/',
  User = 'users/',
  Login = 'auth/login/',
  Validate = 'auth/validate/',
}
