export enum Pathnames {
  Dashboard = '/dashboard',
  Homepage = '/',
  Login = '/login',
  Profile = '/profile',
  Register = '/register',
}

export enum Endpoints {
  Contact = 'contacts',
  Login = 'auth/login',
  Profile = 'users/profile',
  User = 'users',
  Validate = 'auth/validate',
}

export enum AppVariables {
  KeyToken = 'contacthub:auth:token',
}
