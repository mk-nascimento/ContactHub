export enum Pathnames {
  Dashboard = '/dashboard',
  Homepage = '/',
  Login = '/login',
  Profile = '/profile',
  Register = '/register',
}

export enum Endpoints {
  Contact = 'contacts/',
  User = 'users/',
  Login = 'auth/login/',
  Validate = 'auth/validate/',
}
